// TODO: Import PG module to access database

class Repo {

    static InsertUnique(values, columns, table, callback){
        // TODO: Make SQL clauses
    }

    static Select(values, columns, table, callback){
        // TODO: Make SQL clauses
    }

    static Delete(values, columns, table, callback){
        // TODO: Make SQL clauses
    }
}

module.exports = Repo